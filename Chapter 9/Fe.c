#include <stdio.h>
int main()
{
    int max = 5;
    float arr[max];
    // for (i = 0; i < max; i++)
    // scanf("%f", &arr[i]);
    return 0;
}

// Output----->Error as i is undeclared and can not use some variable as length of an array