#include <stdio.h>
int main()
{
    int a = 40;
    if (a > 40 && a < 45)
        printf("a is greater than 40 and less than 45\n");
    else
        printf("%d\n",a);
    return 0;
}

//Answer-------->40 would be printed;